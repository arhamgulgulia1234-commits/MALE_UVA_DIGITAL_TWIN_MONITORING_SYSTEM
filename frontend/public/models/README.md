Empty for now. `components/dashboard/EngineCutaway3D.tsx` currently builds a stylized
procedural cutaway from react-three-fiber primitives (no imported asset). If a real
engine GLTF/GLB model becomes available later, drop it here and swap the primitive
cylinder-bank geometry for a loaded model in that component.
