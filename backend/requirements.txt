fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
websockets==13.1
python-multipart==0.0.9

# Phase 2 — physics simulation, digital twin, PHM/ML layer
numpy==2.1.3
scikit-learn==1.5.2
joblib==1.4.2
matplotlib==3.9.2

# Phase 3 — mission persistence, replay and reporting
SQLAlchemy==2.0.36

# Phase 4 — operating-point optimisation.
# SciPy already arrives as a scikit-learn dependency, but Phase 4 imports it directly
# (scipy.optimize.minimize), so it is pinned here rather than relied on transitively.
scipy==1.14.1
